# 2026-09-06 推荐训练与测试准备记录

- 记录用途：计划制定期间的只读检查与历史证据索引，不是新训练实验的结果。
- 检查日期：2026-09-06，Asia/Shanghai；环境为瞬时观察，实际运行前需重新采样。
- 执行计划：[推荐系统训练、测试与迭代计划](../../../docs/14-推荐系统训练测试与迭代计划.md)。
- 当前状态：环境与旧模型包检查已完成；统一指标采集器、代码修复、新训练和 HTTP 闭环尚未执行。

## 1. 本次实际检查

| 项目 | 结果 | 判断边界 |
|---|---|---|
| Docker Engine | 29.2.1，可访问 | 最初沙箱拒绝访问管道，随后只读权限检查成功 |
| LinkVerse MySQL / Redis / RabbitMQ / Nacos | 四个容器均 healthy | 不等同于 Java 和 Python 应用就绪 |
| 其他 Docker 项目 | 另有项目运行 | 未停止、重启、修改或清理它们 |
| GPU | RTX 4070 SUPER，12,282 MiB；已用 2,236 MiB；使用率 28% | 是一次采样，不是训练峰值 |
| GPU 驱动 | 596.49 | 从 nvidia-smi 读取 |
| CPU / 主机内存 | 24 个逻辑处理器，63.1 GiB | 从系统只读信息读取 |
| Docker 可见资源 | 24 CPU、33,200,164,864 bytes 内存，约 30.9 GiB | 为 Docker 视图，不是容器已占用总量 |
| D 盘剩余 | 193.1 GiB | Python 磁盘检查结果 |
| Python | 3.12.14，项目 `.venv-conda` | CUDA 可用 |
| JDK | Microsoft OpenJDK 21.0.12+8 | `D:\Java JDK\jdk-21.0.12+8` 的 java 已实际输出版本 |
| k6 | PATH 和已知 `.runtime` 工具目录未找到可执行文件 | 不能据此断定整台机器没有；执行前定位或准备锁定版本 2.2.0 |
| 四个 Java 应用健康检查 | 18080～18083 端点均在 2 秒内未成功返回 | 尚未确认就绪；本次没有启动或重启服务 |
| Recommendation readiness | 18084 端点在 2 秒内未成功返回；容器列表无本项目推荐容器 | 不能把 Docker 已开启当作模型已加载 |

运行时依赖实测：PyTorch 2.9.0+cu130、CUDA 13.0、pandas 2.3.2、Faiss 1.12.0、LightGBM 4.6.0、Optuna 4.5.0。

容器的一次资源快照：

| 容器 | CPU | 已用内存 |
|---|---:|---:|
| linkverse-mvp-mysql | 0.33% | 450.4 MiB |
| linkverse-mvp-redis | 2.15% | 11.14 MiB |
| linkverse-mvp-rabbitmq | 0.86% | 171.8 MiB |
| linkverse-mvp-nacos | 0.71% | 1.354 GiB |

以上不是性能基线。正式训练和压测需要按计划持续采样，并记录共同负载。

## 2. 代码与产物身份

检查时 Git HEAD：`6f240414c96b83076a96360a1117a2b1bfd7f345`。

以下两份用户已有未提交修改被保留，本次没有编辑：

| 文件 | SHA-256 |
|---|---|
| `backend/platform-starter-security/src/main/java/ning/linkverse/security/jwt/LinkVerseJwtProfileValidator.java` | `0A46DB0AB444303E9D20A279A6F984C48965359BA3535B4272C7200DD7A8E983` |
| `scripts/_infrastructure.ps1` | `DBFDBBD6F6F77CCA6E978182BC4D9938FB4D735B98C6FC87F4CF662281605A13` |

表中文件以 `linkverse-platform/` 为根。源代码摘要不同于 Git 提交号，后续实验必须同时记录两者，避免模型版本字符串掩盖未提交修改。

| 文件 | SHA-256 |
|---|---|
| `recommendation/environment.yml` | `35632EA825ADCADA843FE594053860C555A3D1B7A596F22915F33D83E20738B8` |
| `recommendation/conda-lock.yml` | `06A3F43C800A74F6C39B5E1897C37F4FBAADF3463A9A10B762DBF572856932C7` |
| `recommendation/datasets/trade-synthetic/manifest.json` | `E168A3D66EB027B306B070F86F3D59700A6FCC204C6EE2A69F599556D08C5B96` |
| 初始模型 `manifest.json` | `01AC6D0688AC30C7B92C2B024C5E73B2B42AD9CFD32A23CF5686E958B2AE8C7F` |
| 反馈模型 `manifest.json` | `A24D4C291B5D17E0C419CB507D8DC84A2491EFD361C280F508A7817C5D110DDE` |

两个模型分别位于：

- `recommendation/artifacts/formal-final/before/trade-20260903072306-20c8891e-4f63f5f4/`。
- `recommendation/artifacts/formal-final/after/trade-20260903072831-20c8891e-4f63f5f4/`。

本次对两包执行了现有 `validate_bundle()`，文件哈希、词表及索引一致性均通过。该检查不表示效果或 HTTP 门禁通过，也没有激活模型。

## 3. 历史结果的使用方式

正式历史参考为 [trade-20260903T070731Z](../runs/trade-20260903T070731Z/manifest.yaml)。更早的 `trade-20260903T060203Z` 已作废，不作为质量对照。

| 已保存证据 | 历史结果 | 本轮使用方式 |
|---|---|---|
| 模型卡双塔三种子均值 | 0.006485 → 0.007845 | 已发现评估模式问题，仅作历史记录；R1 校正后重新建立基线 |
| LambdaRank 验证 NDCG@20 | 0.851708 → 0.860429 | 小候选组训练诊断，不能当作最终推荐准确率 |
| 五画像比较 | NDCG@20 均为 0，质量门槛未通过 | 存在空标签及会话定义问题，待修正 |
| 进程内 CPU 基准 | 20 并发、400 次、成功率 100%、p95 7.5082ms、p99 62.9026ms | 仅 Python 进程内推断；不是 HTTP 延迟 |
| 调参与初始训练总耗时 | 约 1,072 秒 | 两个模型行重复引用了同一总耗时，不能相加 |
| 反馈后重训总耗时 | 约 329 秒 | 非后续轮次时长承诺 |

来源：[模型卡](../runs/trade-20260903T070731Z/model-card.md)、[比较报告](../runs/trade-20260903T070731Z/comparison.md)、[进程内基准](../runs/trade-20260903T070731Z/serving-benchmark.json)、[调参表](../runs/trade-20260903T070731Z/tuning-summary.csv)。

前一轮会话曾对最终窗口的 7,572 名有正反馈用户执行只读全量复评，得到 NDCG@20 约 0.00768 → 0.01044、Recall@50 约 4.46% → 5.50%。那次结果没有通过统一实验入口保存完整原始记录，因此本计划将其视为待复现的审查线索，不直接填入新实验的正式指标文件。R1 必须保存可追溯的复评产物。

同样，上一轮会话的 Python 单元测试为 26 项通过；本次计划制定没有重新运行测试，也没有把历史测试数冒充本轮验证。

## 4. 检查命令与结果分类

以下为实际执行操作的脱敏摘要。硬件、容器和应用健康检查不涉及业务写入。

| 操作 | 结果 |
|---|---|
| `docker version --format '{{.Server.Version}}'` | 29.2.1 |
| `docker ps --format '{{.Names}}\t{{.Status}}\t{{.Ports}}'` | LinkVerse 四个中间件 healthy；其他项目输出不写入本文 |
| `docker info --format 'CPUs={{.NCPU}} MemoryBytes={{.MemTotal}}'` | 24 CPU、33,200,164,864 bytes |
| `nvidia-smi --query-gpu=name,memory.total,memory.used,utilization.gpu,driver_version --format=csv,noheader` | 见资源表 |
| `Get-CimInstance Win32_ComputerSystem` | CPU / 物理内存读取成功；初始沙箱调用受限 |
| 项目 Python 导入 torch/pandas/faiss/lightgbm/optuna，并调用 `shutil.disk_usage('D:/')` | 见版本和磁盘记录 |
| `Invoke-WebRequest` 检查本机 18080～18083 `/actuator/health/liveness` 及 18084 `/health/ready`，超时 2 秒 | 五个端点均未验证就绪；错误类型 TaskCanceledException |
| `docker stats --no-stream`，限定四个 LinkVerse 中间件 | 见容器资源表 |
| `Get-Command k6` 和 `.runtime` 下查找 `k6.exe` | 未命中，不是测试失败 |
| 项目 Python 对两个冻结模型执行 `validate_bundle()` | 两包通过 |
| `git rev-parse HEAD`、`git status --short`、`Get-FileHash -Algorithm SHA256` | 见代码与产物身份 |

## 5. 下一步与记录责任

按 R0 先实现指标采集、阶段状态和快照身份记录，随后执行 R1 校正评估。应用服务未就绪和 k6 未定位不妨碍先做数据、模型及组件测试，但必须在 R5/R6 前解决。

后续每轮均使用[迭代模板](../iteration-template.md)。训练、HTTP 测试、故障注入及模型切换一旦开始，就把实际起止时间、命令、退出码、资源和结果记录到相应运行目录，不回写本准备记录为“已通过”。
