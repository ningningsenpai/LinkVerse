# 本轮实验已作废

本运行仅保留为问题审计记录，所有指标和模型均不得用于比较、晋级或质量结论。

作废原因：

1. 历史负样本池可能包含样本发生时刻尚未发布的商品，违反时点一致性约束。
2. LambdaRank 训练阶段和在线候选阶段的第一项特征含义不一致。
3. 重排输入没有严格限制为精排后的前 50 个候选。

上述问题已在修正后的正式运行 `trade-20260903T070731Z` 中处理并重新完成训练、调参、
模型包校验与画像回放。此目录不会加载或发布对应模型。
